git clone git@github.com:twoturtles/minerl.git minerl-macos
cd minerl-macos
uv venv -p 3.9
source .venv/bin/activate
uv pip install pip
pip install -r requirements.txt
/usr/libexec/java_home -V
export JAVA_HOME=`/usr/libexec/java_home -v 1.8`
java -version
# Try if something doesn't work:
# rm -rf ~/.gradle
bash scripts/setup_mcp.sh
bash scripts/patch_mcp.sh
cd minerl/MCP-Reborn
# Repeat downloadAssets as necessary. It fails regularly.
./gradlew downloadAssets
cd ../..
python unpack_assets.py
cd minerl/MCP-Reborn
./gradlew clean build shadowJar
cd ../..
python setup_min.py install
# Ignore error:
# error: Could not find suitable distribution for Requirement.parse('minerl==1.0.2')

